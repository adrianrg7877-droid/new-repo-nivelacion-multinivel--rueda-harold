pyton:
     esta_vivo = 'true'       # error: es un error de tipo logico ya que los 
                                       valores booleanos en puton son con mayuscula 
                                       inicial y sin comillas
     mana = int('cien')       # error: aqui el error es que  la funcion int() solo 
                                       puede convertir cadenas que representen numeros
                                        validos asi que en ves de "cien" deberia estar 
                                        escrito en numero como "100"
Python:
  if nivel >= 5 and vida > 0
      print('ok')              # error: falta los dos puntos : al final del if

Python (2):
  clase = 'Guerrero'
  if clase == 'guerrero':      # error sutil: las mayúsculas y minúsculas no coinciden ('Guerrero' y 'guerrero')
      print('es guerrero')
